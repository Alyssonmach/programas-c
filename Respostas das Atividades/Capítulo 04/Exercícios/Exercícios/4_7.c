#include <stdio.h>

int main (void)
{

    //for(int x = 3; x <= 7; x++){
    //for(int x = 3; x <= 23; x+= 5) {
    //for(int x = 20; x <= -10; x -= 6) {
    //for(int x = 19; x <= 51; x += 8) {
        printf("%d ", x);
    }

    return 0;
}
